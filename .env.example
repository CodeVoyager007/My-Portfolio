# Contact form email (https://resend.com -> API Keys)
RESEND_API_KEY=
# Optional: where messages go (defaults to the email in src/data/config.ts)
CONTACT_TO_EMAIL=
# Optional: sender once you verify a domain in Resend (defaults to onboarding@resend.dev)
CONTACT_FROM_EMAIL=
